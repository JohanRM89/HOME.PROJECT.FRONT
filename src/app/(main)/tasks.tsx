import { View } from "react-native";
import { Text } from "react-native-paper";

export default function TasksScreen() {
  return (
    <View style={{ padding: 24 }}>
      <Text variant="headlineMedium">Tareas</Text>
    </View>
  );
}